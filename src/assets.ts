import type { AssetsManifest } from "pixi.js";

export const manifest: AssetsManifest = {
  bundles: [
    {
      name: "bundleName",
      assets: {
        bunny: "/images/bunny.png",
      },
    },
  ],
};
